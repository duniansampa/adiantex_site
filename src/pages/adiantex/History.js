import React, { Component } from "react";
import styled from "styled-components";

export default class History extends Component {
  state = {};
  render() {
    return <p>{" Nossas histórias "}</p>;
  }
}
